export const QuizStatus = Object.freeze({
    SAVED: 0,
    PUBLISHED: 1
});

export const AiStatus = Object.freeze({ 
    AI_FREE: 0,
    CONTAINS_AI: 1
});

export const QuizRating = Object.freeze({
    MINIMUM_RATING: 1,
    MAXIMUM_RATING: 5
});