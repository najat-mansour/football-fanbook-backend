export const UserImageStatus = Object.freeze({
    VALID: 1,
    INVALID: null
});

export const AppRating = Object.freeze({
    INITIALIZED_VALUE: 0,
    MINIMUM_RATING: 1,
    MAXIMUM_RATING: 5
});

export const NotificationStatus = Object.freeze({ 
    OFF: 0,
    ON: 1
});

export const ScorePoints = Object.freeze({ 
    PUBLISH_QUIZ: 10,
    SOLVED_ITS_OWN_QUIZ: 1
});